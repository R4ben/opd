import 'package:flutter/material.dart';

class FavoritesNoSession extends StatelessWidget {
  const FavoritesNoSession({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Iniciar Sessao')),);
  }
}
