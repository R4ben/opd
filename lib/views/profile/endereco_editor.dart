import 'package:flutter/material.dart';

class EnderecoEditor extends StatelessWidget {
	const EnderecoEditor({super.key});

	@override
	Widget build(BuildContext context) {
		return Container(

		);
	}
}
